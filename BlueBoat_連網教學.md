# 0. 前言

大家好，我是 Benson，今天要來和大家看 **BlueBoat 無線無人船操作手冊**！

這次的文件是為了 2026 首屆台灣主辦的海洋無人載具演示賽 TMARS 而做，若有疏漏或是錯誤，敬請指正。BlueBoat 是一艘設計優異且功能強大的無人表面載具（USV）。具備高度模組化的機構、極佳的載重能力，以及基於開源生態系（BlueOS & ArduPilot）的強大擴充性。無論是用於水深測量、水質監測、水下聲納掃描，或是各類水面自動航行實驗，BlueBoat 都能提供極高穩定度與靈活度，是水域作業與研究的絕佳利器。

---
### 關於本手冊

* **這是一份實戰操作筆記：**
本手冊旨在記錄從零開始將電腦、基地台與 BlueBoat 建立連線，並順利完成基礎測試的完整流程。
* **新手友善：**
為了讓大家能順利上手，本手冊會盡量避免一開始就塞入大量枯燥的底層網路理論，遇到不想看的章節或是只是想要開船，可以直接跳過說明區。若有錯漏，也歡迎指出！

---

# 1. BlueBoat 的通訊種類

### 1.1 什麼是地面基地台（BaseStation）？

地面基地台（BaseStation）是地面控制站（筆電）與 BlueBoat 之間的**中繼無線通訊樞紐**。

* **外觀與構造：**
具有 IP67 防水防塵外殼，藍色兩邊中間黑色，還插了一根天線。內建高容量鋰電池、MikroTik 高功率長距離無線網橋（Radio），以及 USB/Wi-Fi 轉接模組。
* **核心功能：**
BlueBoat 在水面上運行時，無法直接用常規 Wi-Fi 連接電腦。BaseStation 會在岸邊接收筆電傳出的控制指令，並透過其高功率天線，以 **2.4GHz 無線網橋** 投射出數公里的通訊範圍，達成長距離控制與實體影像/遙測回傳。

---

### 1.2 三段通訊架構

```
┌─────────────┐   USB-C 或 Wi-Fi    ┌──────────────────┐    長距離無線網橋     ┌─────────────────┐    內部實體線    ┌─────────────────┐
│  控制端筆電  │ ──────────────────► │   地面基地台      │ ═══════════════════► │   BlueBoat 船載 Radio  │ ──────────────► │ 樹莓派 (BlueOS)  │ ──► 飛控板 (Autopilot)
│(192.168.2.1)│                     │ (BaseStation)    │     (2.4GHz Wi-Fi)    │ (192.168.2.3)   │                 │ (192.168.2.2)   │
└─────────────┘                     └──────────────────┘                       └─────────────────┘                 └─────────────────┘

```

#### 1. 第一段：筆電 ↔ 基地台 BaseStation

* **連線方式：**
透過買船就有附的 USB-C 實體線，或無線連線至 BaseStation 的 Wi-Fi 訊號（預設 SSID: `BaseStation – 2.4GHz`）。

#### 2. 第二段：基地台 ↔ 船載 Radio

* **連線方式：**
兩端高功率網橋預設已在出廠時完成加密對連（透明橋接），運作時可以視作一條看不見的超長網路線。

#### 3. 第三段：船載電腦 ↔ 航模飛控板

* **連線方式：**
船體內部樹莓派（BlueOS，IP: `192.168.2.2`）經由 USB 介面連結飛控板，負責下達推進器與舵機指令。

---

### 1.3 基地台（BaseStation）開機與連接步驟

#### 0. 關鍵安全與防呆說明：

* **未接天線嚴禁開機：**
務必先將長天線旋緊於 BaseStation 側邊介面。無天線通電會導致內部無線發射模組瞬間過熱燒毀。

> ## **思考時間**
> 
> 
> 在高頻電路中，射頻發射晶片與天線都有一個標準阻抗（通常是 $50\ \Omega$）。天線的角色，就是把晶片產生的電磁波能量，順暢地吸走並發射到空中。
> 
> 如果沒接天線直接開機，晶片面對的是空氣（無限大阻抗，相當於開路）。發射晶片源源不絕輸出的高功率能量完全無法被釋放，這些能量會像水流撞到死胡同一樣 100% 反彈（反射波）回發射晶片，能量瞬間轉化成劇烈高溫，秒殺內部的功率放大器（PA）晶片！

* **USB-C 方向性：**
2024 年 3 月以前生產的 BaseStation 內部 USB 晶片未配對 C-to-C 自動識別線路。若連接筆電後電腦未偵測到新網路裝置，請將 **USB-C 插頭拔下轉 180 度重新插回**。

#### 1. 安裝與調整天線：

將隨附的 2.4GHz 天線旋緊至 BaseStation 的 N 型天線接頭上，並將天線垂直拉直（垂直偏振效能最佳）。

#### 2. 按壓電源開關啟動：

按壓 BaseStation 面板上的電源鍵，確認頂部藍色/橘色指示燈與電池電量條亮起，代表基地台系統開機完成。

---

### 1.4 BlueBoat 電池安裝與開機步驟

#### 0. 電池安裝關鍵規定：

* **測網僅需安裝單顆電池：**
BlueBoat 為雙船體（Catamaran）設計，但**全船電力僅由「右舷艙體（Starboard / 右側）」的單顆 14.8V (4S) 鋰電池提供**。
* **不必插入第二顆電池：**
跨船體的通訊纜線（Crossbar Cable）已將電力導引至左舷的航行與傳感器系統，**切勿在左舷艙體額外插接第二顆電池**。

#### 1. 開啟右舷（右側）防水艙門：

解開右舷艙蓋上的閂鎖，順向打開艙蓋。確認內部密封橡膠圈乾淨、無砂石附著。

#### 2. 安裝與固定單顆 4S 鋰電池：

將 14.8V (4S) 鋰電池放入右舷電池架，用魔鬼氈帶牢牢固定。把電池主電源線插上 **XT90 接頭**，並將**平衡頭（Balance Connector）** 插入旁邊的電壓偵測分線板。

#### 3. 鎖緊艙蓋並啟動電源：

蓋上艙蓋，按壓每一個閂鎖直至發出「咔噠」聲。將 BlueBoat 外側的旋轉式防水電源開關（Rotary Switch）順時針轉至 **「ON」**。

#### 4. 確認系統啟動燈號與音訊：

通電後數秒內，船內蜂鳴器會發出「嗶——」啟動聲，船體頂部的**導航燈（Navigation Lights）亮起**，代表 BlueOS 與飛控系統已成功啟動。

---

# 2. BlueBoat 的各種 IP 與電腦設定

在確認硬體通電與開機無誤後，接下來要讓你的筆電跟 BaseStation 正式「建立通訊」。

---

### 2.1 系統 IP 位址配置表

BlueBoat 系統預設運作在 **`192.168.2.X`** 網段。你可以把 `192.168.2` 當作是「大樓地址」，而最後一個數字就是每台設備的「門牌號碼」：

* **控制端筆電（`192.168.2.1`）：**
你的電腦門牌。我們會手動把網卡設定成這個固定數字，讓電腦知道自己在這條網路線上。
* **BlueBoat 船載電腦 BlueOS（`192.168.2.2`）：**
BlueBoat 的腦袋（樹莓派）。當我們在瀏覽器輸入 `192.168.2.2` 時，就能直接打開 BlueBoat 的控制後台，或是讓 QGroundControl (QGC) 地面站連上船。
* **BaseStation / 船載 Radio（`192.168.2.3`）：**
無線無線網橋（路由器）的門牌。出廠時就已經設好，平時開船時我們**不需要**主動進入這個 IP，它會自己在背景幫我們傳輸資料。

---

### 2.2 電腦端事前設定（以 Windows 為例）

#### 0. 步驟 1：防火牆設定（避免封包被擋）

開啟 **Windows Defender 防火牆** ➔ 選擇「允許應用程式通過防火牆」 ➔ 找到 **QGroundControl**，務必將「私有」與「公用」兩個核取方塊都勾選。

#### 1. 步驟 2：設定筆電固定 IP (Static IP)

1. 按下 `Win + R` 輸入 `ncpa.cpl` 開啟「網路連線（變更介面卡設定）」。
2. 找到你要連接 BaseStation 的網卡：
    * **實體線連接：** 選擇 USB 乙太網路卡（通常顯示為 Realtek USB GbE...）。
    * **Wi-Fi 連接：** 選擇 Wi-Fi 網卡。

3. 在該網卡上點右鍵 ➔ **屬性** ➔ 點選 **網際網路協定版本 4 (TCP/IPv4)** ➔ **屬性**。
4. 選擇「使用下列的 IP 位址」，並填入：

    * **IP 位址：** `192.168.2.1`
    * **子網路遮罩：** `255.255.255.0`  

    當然，你也可以直接在 wifi 介面去做修改，會更簡單。

5. 按下確定儲存。

---

### 2.3 將筆電與 BaseStation 建立連線

完成上述電腦配置後，現在可以將筆電與基地台連起來了（二選一即可）：

* **方式 A（USB-C 線）：**
用 USB-C 線連接筆電與 BaseStation。*(提醒：若電腦沒反應，請將 USB-C 插頭轉 180 度重插)*
* **方式 B（Wi-Fi 無線）：(推薦！）**
開啟筆電 Wi-Fi，搜尋並連線至 `BaseStation – 2.4GHz`（預設密碼：`BaseStation`）。

---

### 2.4 任務結束：將電腦網路設定還原（重要！）

當完成測試、不需要連線 BaseStation 時，**務必將網卡改回「自動取得 IP」**，否則該網卡連接其他網路時將無法正常上網。

#### 還原步驟：
1. 按下 `Win + R` 輸入 `ncpa.cpl` 開啟「網路連線」。
2. 找到剛才設定的網卡（乙太網路或 Wi-Fi），點右鍵 ➔ **屬性**。
3. 點選 **網際網路協定版本 4 (TCP/IPv4)** ➔ **屬性**。
4. 改選為 **「自動取得 IP 位址」** 與 **「自動取得 DNS 伺服器位址」**。
5. 按下確定儲存即可恢復正常上網。

> ## **思考時間**
> 
> 
> ### 1. BlueBoat 系統的 IP 角色分工
> 
> 
> 在 BlueBoat 系統中，設備的 IP 分配如下：
> | 設備名稱 | IP 位址 | 角色說明 |
> | --- | --- | --- |
> | **控制端筆電** | `192.168.2.1` | 地面控制站（執行 QGroundControl / 瀏覽器） |
> | **BlueBoat (BlueOS)** | `192.168.2.2` | 船載主電腦（樹莓派 + 飛控板） |
> | **岸上基地台** | `192.168.2.3` | MikroTik 高功率無線網橋（提供長距離微波通訊） |
> 
> 
> **Q：在正常操作 BlueBoat 時，需要進入 `192.168.2.3` 嗎？**
> **答案是：完全不需要！**
> 
> `192.168.2.3` 是船上搭載的 MikroTik 無線路由器預設管理 IP。出廠或初始化時，兩端的無線參數已設定完成，它會自動將 BaseStation 與船上的樹莓派連接起來，扮演「隱形橋樑」的角色。**只有在無線訊號異常、配對徹底遺失，或需要手動變更 Wi-Fi 頻道與國家法規設定（Regulatory Domain）等進階除錯時，才需要進入該 IP。** 由於 BlueBoat 曾經就有人設定過，因此應該不需要處理這部分，詳細資料可以查看 [https://bluerobotics.com/learn/blueboat-software-setup/#blueboat-and-basestation-pairing](https://bluerobotics.com/learn/blueboat-software-setup/#blueboat-and-basestation-pairing)
> 
> ---
> 
> 
> ### 2. 什麼是子網路遮罩？
> 
> 
> 子網路遮罩（Subnet Mask）的作用是定義「分區規則」，劃分哪些數字代表「區域號碼（網段）」，哪些數字代表「個人門牌（主機 IP）」。
> 想像你買下了擁有 256 個門牌號碼的建案土地（`192.168.2.0` ～ `192.168.2.255`）：
> * **情境 A：蓋一個大社區（遮罩 `255.255.255.0`）**
> 代表不蓋內牆，前三組數字 `192.168.2` 被鎖定為區域號碼，最後一組數字可自由分配給 254 間房子（`.1` 到 `.254`）。筆電（`.1`）走幾步就能直接找 BlueBoat（`.2`）串門子，通訊非常自由。
> * **情境 B：改蓋兩個獨立社區（遮罩 `255.255.255.128`）**
> 數字 `128` 就是砌在中間的高牆，將社區平分為「`.1`~`.126`」與「`.129`~`.254`」兩區。若筆電在 `.1`、BlueBoat 在 `.130`，兩者會因為被高牆隔開而無法直接通訊！
> 
> 
> ---
> 
> 
> ### 3. 為什麼遮罩數字只有 9 種？
> 
> 
> 子網路遮罩在電腦底層是一串 32 位元的二進位數字，規則是 **「左邊必須全是 1（代表網路），右邊必須全是 0（代表主機），1 與 0 中間不能交錯」。因此，遮罩的每一個段落（8 個位元）中，只可能出現以下 9 種十進位數字** ：
> | 二進位排列 (8 bits) | 十進位數值 | 意義與用途說明 |
> | --- | --- | --- |
> | `0000 0000` | **0** | 全開放，不鎖定任何位元 |
> | `1000 0000` | **128** | 切成 2 個子網段 |
> | `1100 0000` | **192** | 切成 4 個子網段 |
> | `1110 0000` | **224** | 切成 8 個子網段 |
> | `1111 0000` | **240** | 切成 16 個子網段 |
> | `1111 1000` | **248** | 切成 32 個子網段 |
> | `1111 1100` | **252** | 切成 64 個網段（每區 4 個 IP，專用於路由器對連） |
> | `1111 1110` | **254** | 極少使用 |
> | `1111 1111` | **255** | 完全鎖定該位元 |
> 
> 
> *(註：像 `253` 或 `100` 這類二進位 1 與 0 交錯出現的數字，絕對不能作為子網路遮罩。)*
>
> ---
> 
> 
> ### 4. 計算切分區域：
> 
> 
> 電腦是用二進位（$2^n$）切割網路，若覺得十進位不直觀，記住魔法數字 **$256$** 即可：
> 
> $$\text{每個子網段的空間大小} = 256 - \text{遮罩最後一位數字}$$
> 
> 
> * **遮罩為 255.255.255.0：**
> $256 - 0 = 256$ $\rightarrow$ 整個空間不蓋牆（1 大段，容納 254 台設備）。
> * **遮罩為 255.255.255.128：**
> $256 - 128 = 128$ $\rightarrow$ 每個社區 128 個空間，$256 \div 128 = 2$（切成 2 段）。
> * **遮罩為 255.255.255.192：**
> $256 - 192 = 64$ $\rightarrow$ 每個社區 64 個空間，$256 \div 64 = 4$（切成 4 段）。
> * **遮罩為 255.255.255.252：**
> $256 - 252 = 4$ $\rightarrow$ 每個社區僅 4 個空間，$256 \div 4 = 64$（切成 64 段微型專線網段）。
> 
> 
> **為什麼要切劃分不同的子網路？**
> 1. **避免廣播風暴（Broadcast Storm）：** 若將上萬台設備放在同一個大網段，發送一次搜尋廣播就會讓全網卡死。
> 2. **網路安全隔離：** 企業可藉此分開「辦公區 (`192.168.10.X`)」，「訪客 Wi-Fi (`192.168.20.X`)」，防止訪客讀取內部伺服器。
> 
> 
> ---
> 
> 
> ### 5. AND 邏輯運算與 Ping 測試
> 
> 
> 當筆電（`192.168.2.1`）要傳送資料給 BlueBoat（`192.168.2.2`）時，電腦會在背景進行 **AND 邏輯運算**（規則：$1 \text{ AND } 1 = 1$，其餘皆為 $0$）：
>
> 192(11000000) AND 子網路遮罩 255(11111111) 得出 網段名稱 192(11000000)
>
> 兩邊計算出來的網段相同時，電腦便會判定：**「是同房間的自己人，直接把封包發過去！」**
> 
> **設錯遮罩會發生什麼事？**
> 若誤將筆電遮罩設為 `255.255.255.255`，電腦算出的 BlueBoat 網段會變成 `192.168.2.2`，與筆電自身的 `192.168.2.1` 不符。筆電會誤以為 BlueBoat 在網際網路外網，硬把封包丟給不存在的閘道器（Gateway），導致連線逾時（Timeout）失敗。

---

# 3. 連線驗證與 BlueOS 後台登入

完成硬體開機與電腦 IP 設定後，本章將帶你驗證實體與無線網路通訊是否正常，並正式登入 BlueBoat 的主控系統 **BlueOS**。

---

### 3.1 網路連線 Ping 測試

在打開瀏覽器或地面站軟體前，最快速且準確的方法是使用命令提示字元（Command Prompt）發送 ICMP 封包，測試筆電與 BlueBoat 之間的連線狀況。

#### 1. 開啟命令提示字元：

按下 `Win + R`，輸入 `cmd` 後按下 Enter。

#### 2. 測試對船載電腦（BlueOS）的連線：

在黑底視窗中輸入以下指令並按下 Enter：

```bash
ping 192.168.2.2
```

* **正常連線回覆：**  
如果看到 `來自 192.168.2.2 的回覆: 位元組=32 時間<10ms TTL=64`（Reply from 192.168.2.2...），代表筆電與 BlueBoat 之間的通道已完全打通！
* **異常情況與排錯（Request Timed Out / 要求的等候逾時）：**
    * **檢查筆電 IP：** 確認網卡 IP 是否已設定為 `192.168.2.1` 且子網路遮罩為 `255.255.255.0`。  
    * **檢查實體線/Wi-Fi：** 若使用 USB-C 連接 BaseStation，請拔下插頭旋轉 180 度重插；若使用 Wi-Fi，確認已連上 `BaseStation – 2.4GHz`。
    * **檢查 BaseStation 防呆：** 確認天線已接妥且 BaseStation 電源已啟動。

---

### 3.2 首次進入 BlueOS 控制頁面

BlueOS 是執行於船載樹莓派上的核心管理系統，採用 Web 架構，不需要安裝額外軟體即可透過瀏覽器進行全船設定與狀態監控。

#### 1. 打開瀏覽器：

推薦使用 Google Chrome 或 Microsoft Edge。

#### 2. 輸入網址連線：

在網址列輸入以下任一地址：

* IP 地址方式：http://192.168.2.2
* 主機名稱方式：http://blueos.local

#### 3. 熟悉 BlueOS 主介面：

成功進入後，畫面會顯示 BlueOS 儀表板，可以試著檢查以下項目：

* **Autopilot 狀態：** 確認畫面頂部或邊欄顯示 `Connected`，代表樹莓派已成功連上 ArduPilot 飛控板。
* **系統資源：** 檢視 CPU 使用率、記憶體與運作溫度（建議運作溫度維持在 65°C 以下）。
* **電源管理 (Power)：** 觀察即時電壓數據（4S 鋰電池滿電約為 16.8V，單顆 cell 低於 3.5V / 總電壓低於 14.0V 時需準備充電）。

**在第一次使用前務必更新，要尋找通訊埠、MAVLink 轉發路由或實體接口時，請開啟右上的 "Pirate Mode"**

---

> ## **思考時間**
> 
> 
> ### 1. 為什麼 Ping 能夠測試網路通訊？
> 
> 
> `ping` 指令利用的是 TCP/IP 網路通訊協定中的 **ICMP (Internet Control Message Protocol)** 協定。
> 當你輸入 `ping 192.168.2.2` 時：
> 1. 筆電會發射一個 `Echo Request`（請求回應）封包給 BlueBoat。
> 2. BlueBoat 的樹莓派收到後，必須立即回傳一個 `Echo Reply`（應答）封包。
> 
> 
> 如果能收到回應，代表兩者之間的「物理線路/無線網橋」、「IP 網段配置」以及「底層網卡驅動」完全正常。
>
> ---
> 
> 
> ### 2. 為什麼輸入 blueos.local 也能連上？
> 
> 
> 這背後使用的是 **mDNS (Multicast DNS / 多播功能域名系統)** 與 **Zeroconf (零配置網路)** 技術。
> * 一般上網需要 DNS 伺服器把域名（如 google.com）轉譯成 IP。
> * 在沒有網際網路的封閉區域網路（如 BlueBoat 與 BaseStation 建立的網段）中，BlueBoat 會在區域網路內廣播宣告：「我是 `blueos.local`，我的 IP 是 `192.168.2.2`！」
> * 筆電收到廣播後，自動將 `blueos.local` 綁定至該 IP。即便忘記 IP，輸入域名也能順利連線。
> 
>

---

# 4. QGC 地面站對接與系統校正

在完成網路連線與 BlueOS 登入後，我們需要使用 **QGroundControl (QGC)** 地面控制站軟體來接收船體的遙測數據（如 GPS 位置、姿態、電量），並下達控制指令與規劃任務。

---

### 4.1 建立 QGC 與 BlueBoat 的連線

1. **開啟 QGC 軟體：**  
確認 BlueBoat 與 BaseStation 皆已通電開啟，且電腦已連上網路，啟動 QGroundControl。
2. **自動連線與手動檢查：**
    * **自動連線：** QGC 開啟後，等待幾秒鐘，BlueBoat 將會自動連線，畫面頂部會出現綠色的參數載入進度條。
    * **若未自動連線（手動檢查）：** 請確認 BlueOS 內部的 MAVLink Endpoints 是否正確將 UDP 封包轉發至 `Port 14550`，以及防火牆和電腦的 IP。

---

### 4.2 感測器校正（Sensor Calibration）

如果是第一次設定 BlueBoat，系統會提示需要進行額外設定。請點擊左上角的 **Q 圖示** ➔ **Vehicle Setup**，準備進行校正：

1. **加速度計與羅盤校正：**
* 點擊左側欄的 **Sensors**。
* 進行 Accelerometer（加速度計）與 Compass（羅盤）校正時，務必確保 **Autopilot Rotation** 的設定值為 **「Roll180」**，然後點擊 Ok 並依照畫面指示旋轉船體。在設定加速度計時務必要按下 next 以切換姿態。
* 羅盤校正容易受到附近鐵磁金屬影響，如果校正結果不佳，請移至空曠處重試。

---

### 4.3 馬達轉向檢查（Check Motor Directions）

### **警告：在檢查前，請確保螺旋槳周圍沒有人員、狗勾或任何障礙物！且千萬不可在無水狀態下高速乾轉馬達過長，以免損壞軸承。**

1. 進入 **Vehicle Setup** ➔ 點擊左側欄的 **Motors**。
2. 開啟馬達滑桿的啟用按鈕。
3. 調整至你想要的速度比例後，點選以下兩者之一：
    * **滑桿 C** 控制左舷（Port side）馬達，從船尾往前看，應為 **逆時針旋轉**。
    * **滑桿 D** 控制右舷（Starboard side）馬達，從船尾往前看，應為 **順時針旋轉**。

---

### 4.4 解鎖前安全檢查（Pre-Arm Checks）

下水執行任務前，請透過 QGC 狀態列進行最後確認：

* **GPS 衛星定位：** 確認衛星數量充足（建議 > 12 顆），且 HDOP 數值良好（< 1.2）。
* **電池電壓：** 確認左上角的電壓顯示，4S 電池滿電為 16.8V，若降至 12V 則視為完全耗盡，**絕對不可讓電池電壓低於 12V** 以免造成永久損壞。

---

# 5. 故障保護與安全維護

### 5.1 故障保護機制 (Failsafes)

故障保護是 BlueBoat 遭遇特定異常（如斷訊或低電量）時自動執行的保護措施。相關行為可於 BlueOS 的「自動駕駛參數」或 QGroundControl (QGC) 的「車輛設定」➔「參數」中配置。

#### 1. 地面控制站（GCS / 遙測）故障保護

當船隻與地面站電腦失去通訊（停止接收心跳訊息）超過設定時間時觸發：

* **觸發時限：** 由 `FS_TIMEOUT` 控制，預設值為 **5 秒**。
* **非自動模式中斷線：** 切換為「保持（Hold）」模式，停止馬達運轉。船隻保持待機狀態，不嘗試維持 GPS 位置（隨波漂流）。
* **自動模式中斷線：** 預設忽略故障保護，確保自主任務不中斷執行（需確保預設任務路線最終會將船隻帶回通訊範圍內）。
* **開關控制 (`FS_GCS_ENABLE`)：**
    * `0`：停用 GCS 故障保護。
    * `1`：觸發時執行 `FS_ACTION` 設定動作。
    * `2`：自動模式下繼續執行任務，其餘模式執行 `FS_ACTION`。

* **動作配置 (`FS_ACTION`)：**
    * `0`：不執行任何操作。
    * `1`：啟動 RTL（返回啟動點）。
    * `2`：啟動保持（Hold）模式（停機隨波漂流）。
    * `3`：優先啟動 SmartRTL，無效時執行標準 RTL。
    * `4`：優先啟動 SmartRTL，無效時切換為 Hold。

#### 2. 電池故障保護

當電池電壓過低時自動保護電池與載具（長途跨點任務且超出通訊範圍時建議停用）：
* **觸發條件：** 電壓低於 `BATT_LOW_VOLT` 設定值且持續時間超過 `BATT_LOW_TIMER`（預設 **10 秒**）。

* **參數設定建議：**
    * 啟用時，應將 `BATT_FS_VOLTSRC` 設為 **「電壓驟降補償電壓」（值為 1）**。
    * 官方手冊明確建議：**不建議使用基於容量的觸發機制**，應以電壓為準。

> ## **思考時間**
> 
> 
> ### 1. 什麼是「電壓驟降（Voltage Sag）」？
> 當電池處於靜止（沒推油門）時，電壓通常比較高。但當你突然猛推油門或逆風全速衝刺時，馬達瞬間抽取大量電流，受電池內部電阻（內阻 $R_{int}$）影響，測得的端電壓會瞬間大幅下掉（例如從 14.8V 瞬間掉到 13.5V）。當油門放鬆後，電壓又會彈回 14.6V。
>
> ### 2. 為什麼需要「補償」？
> **如果使用原始電壓（Raw Voltage）：**
> 在電池還有不少電的情況下，只要一次全速前進引起瞬間電壓驟降，系統可能就會誤以為電池見底，直接觸發故障保護（把船開回家或直接停機）。
>
> **使用電壓驟降補償電壓（Sag-Compensated Voltage）：**
> 飛控板（Navigator / ArduPilot）會結合當前即時量測到的「電壓」與「消耗電流」，利用電池內阻模型計算出：「如果現在沒有大電流抽載，這顆電池真實的靜態剩餘電壓是多少」。
>
>
>### 因此務必要把補償電壓打開！

* **動作配置 (`BATT_FS_LOW_ACT`)：**
* 可選：`0 (None)`（預設值）、`1 (RTL)`、`2 (Hold)`、`3 (SmartRTL/RTL)`、`4 (SmartRTL/Hold)`、`5 (終止 Disarm)`。

* **二級深度保護：**
    * 次級較低電壓閾值儲存於 `BATT_CRT_VOLT`（預設為 0，即停用）。
    * 二級保護觸發動作儲存於 `BATT_FS_CRT_ACT`（選項同上）。

---

### 5.2 操作安全與公共安全規範

為確保水域人身安全與設備完整，作業現場須嚴格遵循以下規定：

* **人身防護與螺旋槳安全：**
    * 當 BlueBoat 處於**已武裝（Armed）或運作狀態時，人員請務必遠離螺旋槳**。
    * 嚴禁使用 BlueBoat 進行**拖曳或載人**作業。

* **操作與搬運原則：**
    * **只能在 BlueBoat 解除武裝（Disarmed）或完全斷電的情況下**進行搬運、下水或回收。

* **水域與視線規範：**
    * 操作時務必將載具維持在視線範圍（LOS）與無線電通訊範圍內。
    * 船身必須依規定使用**視覺警示旗（Visual Flag）**。
    * 避免在繁忙水域作業。
    * 隨時做好手動接管控制的準備。

---

### 5.3 載具關閉與標準下電流程

完成任務或自水中回收後，請依循標準程序切斷電源：

1. **軟體解除武裝（Disarm）：** 先行透過遙控器或 QGC 地面站將 BlueBoat 解除武裝。
2. **回收船體：** 將船隻從水面移至岸上或甲板。
3. **旋轉實體開關下電：** 將艙蓋外部的穿透式旋轉開關撥至 **「OFF」** 方向，確認航行燈熄滅，代表全船已斷電。
4. **收折防護：** 順向將天線向下折疊並卡入天線夾，解鎖夾緊桿折疊雙體船身以便運輸。


---

### 5.4 任務後保養與器材維護清單 (Post-Mission Checklist)

任務結束後，應立即執行以下維護步驟以防止海水腐蝕並維持硬體壽命：

* **淡水清洗：**
    * 作業結束後**必須使用淡水徹底沖洗全船**。

* **電池取出與儲存保養（超級無敵宇宙重要）：**
    * 若短期內不再使用，必須將電池從船艙內取出。
    * 使用平衡充電器將電池充放電至標準**保存電壓（Storage Charge）**，並存放於安全防護場所。

* **硬體結構與水密性巡檢：**
    * 檢查所有艙壁穿透件，確認安裝牢固且未鬆動。
    * 檢查船架（Frame）與船架支架螺栓是否有鬆脫跡象。
    * 檢查左右推進器螺旋槳是否有缺角、纏繞或外力損壞。


> ## 你知道嗎？
> Armed 與 Unarmed 一詞來自於拉丁文 arma，武裝自己與解除武裝因此直接使用了裝甲一詞作動詞使用。隨著火砲、水雷、航空炸彈與魚雷的出現，「Arm」在軍工領域衍生出關鍵的安全術語 "Arming Mechanism"（引信保險解除機構），因此通電（Power On）只是讓主系統運作，而只有在攻擊前撥開保險蓋、切換至 ARM，高壓點火迴路或拋離炸藥包才會真正通電處於可觸發狀態。由於在軍事與航天的大量運用，因此在這裡也被用來嚴格區分「電路通電」與「動力輸出」的差異。
> 
> 當然，你只要記住，允許馬達輸出動力的狀態時，**推進器已具備物理危險性、切勿靠近槳葉！**

---

# 6. 電池配置、航行控制與下水任務實施

在完成第 1 至 5 章的連網設定、軟體校正與故障防護配置後，我們終於要進入 BlueBoat 的實戰作業階段，包含正式的多電池並聯安裝、配重平衡、QGC 離線地圖與任務規劃，以及現場下水投放、航行模式切換與回收保養的標準作業程序！

---

### 6.1 正式電池安裝、並聯規範與配重平衡

#### 1. 多電池並聯規範與安裝步驟
BlueBoat 支援雙舷安裝，最多可同時連接 **8 顆 Blue Robotics 4S 鋰電池**（左舷與右舷各最多容納 4 顆）。每個船體內部均配置一條 4S 分線器（4S Splitter Cable），採用 XT90 接頭與 5-pin JST XH 平衡線接頭，並且在左右皆有可以固定電池的魔鬼氈帶。

* **並聯安全準則（極為關鍵）：**
  * **規格一致：** 務必使用電壓、安時容量（Ah）與放電倍率相同的 4S 電池。
  * **壓差限制：** 所有並聯電池之間的電壓差異**不得超過 0.2V**。連接前請使用測電器確認各顆電壓。
  * **狀況相近：** 僅並聯使用年限與循環壽命相近的電池。

* **實體安裝與防短路步驟：**
  1. 打開左舷與右舷艙蓋。
  2. 取下預備連接電池之 XT90 接頭上的保護蓋；**其他未使用的 XT90 接頭必須保留保護蓋**，以防金屬接觸造成短路。妥善保管拆下的保護蓋。
  3. 將各電池的主電源 XT90 與平衡接頭分別插上 4S 分線器。分線器電纜應始終置於電池下方，以減少對電子設備的電磁干擾。
  4. 使用艙內魔鬼氈帶牢固固定每顆電池。未固定的電池在航行晃動中可能脫落導致線路短路或斷電。
  5. 蓋上艙蓋，從頂部按壓每個閂鎖直到聽到「咔噠」聲完全卡入鎖定。

* **船內直接並聯充電方法：**
  * 打開右舷艙蓋，斷開交叉電纜 XT90 接頭與「STBD PWR」電纜的連接。
  * 將隨附標有「CHARGE」的額外充電線纜之 XT90 與 8-pin Molex 接頭連接至剛斷開的 XT90 及交叉電纜上的空閒 8-pin Molex 接口。
  * 將充電線另一端的 XT90 與平衡連接器接入充電器（如 H6 PRO）即可直接在船內為並聯電池充電。

#### 2. 有效載重（Payload）與重心配重原則
BlueBoat 的總有效載荷為 **15 公斤（33 磅）**（包含電池與所有外加載荷）。重量分佈直接影響載具在水中的航行穩定性與俯仰姿態：
* **左右平衡：** 電池應盡可能均勻分配於左舷與右舷（例如使用 2 顆電池時，左右舷各裝 1 顆；使用 4 顆時各裝 2 顆）。
* **前後平衡：** 電池裝入船體內部時應前後均勻分佈（如兩顆放在中央，或一前一後）。
* **外加酬載補償：** 若在船體後部安裝重型酬載（如聲納、光達），應將內部電池向前移動以抵消不均勻負載，保持水面吃水平衡。
* **低重心原則：** 酬載設備應盡量安裝在較低位置，避免重心過高導致俯仰與橫滾不穩定性增加。

#### 3. 電池配置與續航力參考表
| 電池數量 (Battery Setup) | 總電量與重量 (Capacity & Weight) | 預估續航時間 (@ 1 m/s, ~30W, 無額外負載) | 預估航程 (@ 1 m/s) |
| :--- | :--- | :--- | :--- |
| **2 顆 4S 電池** | 532 Wh, 2.4 kg | 18 小時 | 65 公里 |
| **4 顆 4S 電池** | 1064 Wh, 4.7 kg | 36 小時 | 130 公里 |
| **6 顆 4S 電池** | 1596 Wh, 7.0 kg | 50 小時 | 180 公里 |
| **8 顆 4S 電池 (滿載)** | 2128 Wh, 9.3 kg | 62 小時 | 220 公里 |

*(註：4S 鋰電池滿電為 16.8V，12V 即為徹底耗盡。絕對不可使電壓低於 12V。)*

---

### 6.2 QGC 離線地圖下載與任務規劃 (Mission Planning)

#### 1. QGC 前置設定（防呆）
點擊 QGC 左上角 **Q 圖示** ➔ **Application Settings** ➔ **General** ➔ 在 **Plan View** 區塊中，勾選 **「Missions Do Not Require Takeoff Item」**。此設定只需執行一次，可避免水面無人船因缺少起飛而無法執行任務。

#### 2. 離線地圖下載步驟 (Downloading Offline Maps)
出海或野外作業通常無行動網路，需預先快取作業水域地圖：
1. 在 QGC 地圖視圖中將畫面移至欲下載的作業區域。
2. 點擊左上角 **Q 圖示** ➔ **Application Settings** ➔ **Offline Maps** ➔ 點擊 **「Add New Set」**。
3. 設定地圖集名稱、選擇地圖類型（如 Google Hybrid 衛星圖），調整所需的最小與最大縮放層級（Zoom Levels）。
4. 點選 **「Download」** 完成圖磚快取。

#### 3. 航點任務與網格勘測任務規劃 (Waypoint & Survey Mission)
* **基礎航點任務 (Waypoint Mission)：**
  1. 點擊 QGC 左側 **Plan** 進入規劃模式。
  2. 在水域地圖上點擊新增航點序列（Waypoints），設定航點半徑 `WP_RADIUS` 與航點速度 `WP_SPEED`。
  3. 最後一個航點可指定為 Return to Launch (RTL)。
  4. 點擊 **「Upload Required」** 將航線寫入 BlueBoat 飛控板（未連線時可存檔至本機稍後上傳）。
* **網格勘測任務 (Survey Mission)：**
  1. 於 Plan 視圖中選擇 **Survey** 工具，框選作業水域邊界。
  2. 系統會自動產生 Takeoff（起點）、網格掃描線與 Return to Launch 指令。可手動調整掃描線間距與航線角度。
  3. 可手動插入常規航點以繞開障礙物，確認無誤後點擊 **「Upload Required」** 上傳。

---

### 6.3 航行控制模式與頂部導航燈閃爍對照

#### 1. 五大常用控制模式 (Control Modes)
| 模式名稱 (Mode) | 說明 (Description) |
| :--- | :--- |
| **手動模式 (Manual)** | 搖桿油門與轉向直接映射至左右推進器差速輸出，不依賴 GPS。強烈建議在控制器上始終配置一個實體切換按鈕。 |
| **特技模式 (Acro)** | 橫向搖桿控制轉彎速率（由 `ACRO_TURN_RATE` 控制），回中時系統自動保持當前航向並補償風浪外力。 |
| **懸停模式 (Loiter)** | 在設定的漂移半徑（`LOIT_RADIUS`，0~20 公尺）內隨波漂移；一旦超出範圍，系統自動轉向並以最高 0.5 m/s 修正回原點。 |
| **自動模式 (Auto)** | 自動依序執行已上傳的航點或 Survey 網格任務。任務中可隨時手動切換至 Manual 接管；重新切回 Auto 會從最後中斷指令繼續執行。 |
| **引導模式 (Guided)** | 於 QGC 地圖上點擊任意位置下達「Click to Go」指令，載具即自主導航前往。 |

#### 2. 船頂導航燈號辨識表 (NavLight Flash Patterns)
| 燈號閃爍模式 (Light Pattern) | 武裝狀態 (Arm Status) | 當前模式 (Control Mode) |
| :--- | :--- | :--- |
| **慢單閃 (Slow Single Pulse, 0.5Hz)** | Disarmed (未解鎖) | — |
| **慢單閃 (Slow Single Pulse, 0.5Hz)** | Armed (已解鎖) | Hold |
| **雙閃 (Double Pulse, 1Hz)** | Armed (已解鎖) | Manual / Acro / Steering / Simple |
| **快三閃 (Fast Triple Pulse)** | Armed (已解鎖) | Auto / Guided / RTL / Smart RTL |
| **常亮 (Solid On)** | Armed (已解鎖) | Loiter |
---

### 6.4 任務前整備、投放發射與任務執行 SOP

#### 1. 出勤攜帶與任務前檢查清單 (Pre-Mission Checklist)
* **法規與環境：** 確認作業水域符合當地 USV 作業法規，避開繁忙航道與衝浪巨浪區。
* **機構與緊固件：** 檢查船架（Frame）與支架螺栓無鬆脫；橫樑管完全套入軟管接頭並夾緊；夾緊桿牢固鎖定（展開狀態不可意外折疊）。
* **推進與水密：** 檢查螺旋槳無損壞、手轉順暢；檢查艙壁穿透孔、空白穿透器、航行燈及穿透開關隔板安裝牢固且水密無虞。
* **天線與電量：** 船載天線豎直拉直；BaseStation 充足電量；所有 4S 電池已滿電且壓差在 0.2V 內。
* **連線測試：** 筆電連接 BaseStation，確認 QGC 連線正常且離線地圖已預載。

#### 2. 投放與發射方式 (Launch Methods)
* **岸邊下水 (Shore Launch)：**
  * 推入水中或手動遙控駛入水域，**確認螺旋槳已完全浸入水中**。
  * 船底龍骨堅固，可承受崎嶇地面，但必須避開水下大石以免撞擊損壞槳葉。
* **高處即時投放 (Instant Launch - 碼頭或母船)：**
  * **高度限制：** 最大允許跌落高度約為 **0.6 公尺（2 英尺）**。過高跌落的衝擊力可能導致夾緊把手鬆脫使船身折疊。
  * **入水角度：** 保持船體約 **45° 入水**。入水角度過陡可能導致船體翻覆。

#### 3. 任務啟動與在水執行
1. 確認船隻在水中後，將控制模式設為 **Manual 模式** 並執行 **Arm（武裝）**。
   *(警告：在 QGC 滑動「Slide to confirm」滑桿會自動武裝並直接啟動自主任務，只有在準備妥當後方可滑動。)*
2. 準備開始任務時，將控制模式切換至 **Auto 模式**，BlueBoat 將自動啟航執行航點。
3. 自主任務完成後，系統會自動執行 RTL 返航並於起點切換為 Loiter 或 Hold 模式等待回收。

---

### 6.5 載具回收與任務後保養 SOP (Recovery & Post-Mission)
1. **航行至回收點：** 任務結束後以手動模式駛近岸邊或回收點（避開大石）。
2. **執行 Disarm：** 準備打撈前，**必須先在 QGC 或遙控器上執行 Disarm（解除武裝）**，切斷推進器動力。
3. **回收與實體斷電：** 將船體搬運上岸，將外側穿透開關順時針撥至 **「OFF」**，確認航行燈熄滅。
4. **折疊收納：** 將天線向下折疊扣入天線夾，解鎖橫樑夾緊桿，將左右船體向內折疊並重新鎖定夾緊桿便於運輸。
5. **淡水清洗與數據導出：**
   * 使用淡水徹底沖洗全船外部與螺旋槳（尤其鹽水作業後）。
   * 導出並備份各項感測器與任務數據。
   * 若近期不再使用，將電池移出船艙，使用平衡充電器充放電至 **保存電壓（Storage Charge）** 並妥善存放。

---

# 7. 進階開發：透過 ROS 2 與 MAVROS 進行無人船外部控制

在完成前述章節的連網、BlueOS 設定、QGC 地面站校正與基本航行測試後，恭喜你已經學會如何操作 BlueBoat 了！  但若是若要進一步實現自主導航演算法、避障（Obstacle Avoidance）或與其他自主機器人框架（如 MOOS-IvP）對接，我們需要將 BlueBoat 接入 **ROS 2（Robot Operating System 2）** 生態系。

本章將詳細介紹 MAVLink、MAVROS 與 BlueOS 之間的系統架構，並說明如何從控制端（Docker 容器）發布速度指令直接操控船隻。**請注意，因為筆者此時尚未有公司 Docker 權限以使用 ROS2，若有錯誤煩請指出。**

---

### 7.1 核心觀念：MAVLink、MAVROS 與 ROS 2 的角色關係

在自主無人船控制架構中，電腦演算法與實體推進器之間採用了**分層式架構**：

1. **BlueBoat 本體（底層）**：機載電腦（樹莓派 + Navigator 飛控板）運行 BlueOS 與 ArduPilot（ArduRover 韌體），內部通訊採用開源無人載具標準的 **MAVLink 二進位協定**。


2. **開發端電腦（上層）**：自主航行與任務演算法通常建構於 **ROS / ROS 2** 環境中，使用 Topics、Services 與 Actions 傳遞資料。


3. **MAVROS（中介橋樑）**：MAVROS（MAVLink to ROS）負責雙向「翻譯」工作：


    * **指令下發**：將 ROS 發出的速度控制指令（`geometry_msgs/Twist`）轉換為 MAVLink 封包（如 `SET_POSITION_TARGET_LOCAL_NED`）下發給飛控板。


    * **遙測接收**：將飛控回傳的 GPS、IMU、姿態與電池狀態解碼為標準 ROS Topics。





```
【控制端電腦 (Docker / ROS 2)】
   │
   │ 1. 發布速度控制主題 (/mavros/setpoint_velocity/cmd_vel_unstamped)
   ▼
【 MAVROS 橋接節點 】
   │
   │ 2. 轉換為 MAVLink 封包 (SET_POSITION_TARGET_LOCAL_NED)
   ▼    (透過 Wi-Fi 網橋 192.168.2.3 / 192.168.2.2)
【 BlueBoat 船載電腦 (BlueOS / 樹莓派 4B)】
   │
   │ 3. BlueOS 路由模組將 MAVLink 轉發給內建 ArduPilot (ArduRover)
   ▼
【 Navigator 飛控板 (VCU) 】
   │
   └─► 4. 輸出 PWM 差速訊號，驅動兩側 M200 推進器旋轉

```

---

### 7.2 BlueOS 端的通訊接口與埠號檢視

在控制端連線前，需確認 BlueOS 上的 MAVLink 轉發端點與系統 Ports：

1. **開啟海盜模式（Pirate Mode）**：
* 連入 http://192.168.2.2 或 http://blueos.local。


* 開啟 **Pirate Mode** 以顯示完整的底層工具選單。


2. **確認 MAVLink 轉發端點（Autopilot Endpoints）**：
* 前往 **Autopilot** ➔ **MAVLink Endpoints**。


* 確認系統已配置將 MAVLink 封包轉發至 UDP `14550` 或 TCP `5777` 服務埠。




3. **查詢實體串口與可用服務**：
* **實體串口**：前往 **Tools** ➔ **Serial Bridges** 查看偵測到的硬體（如 `/dev/ttyUSB0`、`/dev/ttyACM0`）。


* **可用服務列表**：前往 **Tools** ➔ **Available Services** 可檢視系統運行的服務埠（例如 BlueOS Web: 80、Terminal: 8088、MAVLink TCP: 5777）。





---

### 7.3 ROS 2 / Docker 環境下操控 BlueBoat 實戰流程

#### 步驟 1：啟動並進入 ROS 2 Docker 容器

在控制端筆電（如已配置好 MAVROS 與相關環境的容器 `moos-ros2-base:debug`）中確認容器狀態並進入終端：

```bash
# 查看運行中的 Docker 容器
docker ps

# 進入開發容器終端
docker exec -it moos-ros2-base_debug bash
```

#### 步驟 2：檢查 MAVROS 連線狀態與控制主題

確認 MAVROS 成功與船隻建立通訊：

```bash
# 查看連線狀態與當前模式
ros2 topic echo /mavros/state
```

若連線正常，輸出應包含：

```yaml
connected: true
armed: false
mode: MANUAL
```

同時可檢查速度控制主題是否存在：

```bash
ros2 topic list | grep setpoint_velocity
```

常見控制主題包括 `cmd_vel`（含時間戳記 `TwistStamped`）與 `cmd_vel_unstamped`（無時間戳記 `Twist`）。

#### 步驟 3：切換控制模式為 GUIDED（重要）

ArduRover 只有在 **`GUIDED`（引導模式）** 下才會接受外部程式發送的速度與轉向 Setpoint；在 `MANUAL` 模式下直接發布速度指令將不會動作：

```bash
# 呼叫 MAVROS 服務切換模式至 GUIDED
ros2 service call /mavros/set_mode mavros_msgs/srv/SetMode "{base_mode: 0, custom_mode: 'GUIDED'}"
```

回傳 `mode_sent: true` 即代表模式切換請求已送出。

#### 步驟 4：執行載具解鎖（Arming）

在安全確認完成後（無水測試時請確保螺旋槳淨空），透過 Service 發送解鎖指令：

```bash
# 解鎖推進器動力
ros2 service call /mavros/cmd/arming mavros_msgs/srv/CommandBool "{value: true}"
```

再次檢查 `/mavros/state`，確認呈現 `connected: true`、`armed: true`、`mode: GUIDED`。

#### 步驟 5：發布速度指令（Velocity Setpoint）

透過 ROS 2 主題以固定頻率（建議 5~10 Hz）持續發送前進或轉向指令：

* **純前進測試（0.3 m/s）**：
```bash
ros2 topic pub -r 10 \
  /mavros/setpoint_velocity/cmd_vel_unstamped \
  geometry_msgs/msg/Twist \
  "{linear: {x: 0.3, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}"
```



* **前進並帶轉向測試（前進 0.3 m/s，偏航角速度 0.2 rad/s）**：
```bash
ros2 topic pub -r 10 \
  /mavros/setpoint_velocity/cmd_vel_unstamped \
  geometry_msgs/msg/Twist \
  "{linear: {x: 0.3, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.2}}"
```



> ## **思考時間**
> 
> 
> ### 1. 為什麼指令必須加上 `-r 10` 連續發送？
> 
> 
> ArduPilot 安全架構包含 **指令超時保護**。自駕核心收到外部速度目標後，若在短時間內（通常為 1~2 秒）未收到下一筆指令，會判定外部控制節點斷訊或當機，並自動停止推進器運轉，以防載具失控暴衝。
> 
> 
> ### 2. 座標系設定（mav_frame）的影響
> 
> 
> MAVROS 預設速度參考坐標系可能為全域東北地坐標（`LOCAL_NED`）。對於船隻自主運動控制，通常希望速度是相對於「船體自身」（即 `linear.x` 代表船頭前進方向）。
> 可透過以下指令確認並切換至船體座標系：
> 
> 
> ```bash
> # 檢查當前坐標系參數
> ros2 param get /mavros/setpoint_velocity mav_frame
> 
> # 設定為船體坐標系 (BODY_NED)
> ros2 param set /mavros/setpoint_velocity mav_frame BODY_NED
> ```

---

# 8. 範例展演

在大家已經有足夠的知識後，我們將引領你看 MAVROS 可以做到甚麼事情。我們採用 Huang, Yuan Kai 的GitHub專案：**[JetSeaAI/Blueboat](https://github.com/JetSeaAI/Blueboat)**。，來進行技術展示與解說。本章不複製全部程式碼，只提取部分核心重點，方便大家對照 GitHub 原始碼進行開發與擴充。

---

### 8.1 Docker 容器化開發環境 (`moos-ros2-base`)

為了避免因作業系統版本、ROS 2 相依套件或 Python 環境不一致導致的編譯錯誤，專案採用了 Docker 容器化技術。

* **環境路徑**：`Docker/moos-ros2-base/`
* **容器特色**：
    * 預裝 **ROS 2**、**MAVROS**、**Zenoh** 與 **MOOS-IvP** 海洋自律架構環境。
    * 透過 `environment.sh` 與 `/ros_entrypoint.sh` 自動載入所有相依變數與工作空間（`ros2_ws`）。
    * **快速啟動流程**：
```bash
# 1. 進入目錄並啟動容器
cd Docker/moos-ros2-base
docker compose up -d

# 2. 進入開發容器
docker exec -it moos-ros2-base_debug bash
```

---

### 8.2 長距離無線中繼與通訊優化 (`rmw_zenoh` / `zenohd`)

在海上或戶外開闊水域中，船端與地面基地台（BaseStation）透過 2.4GHz 無線網橋傳輸資料。傳統 ROS 2 的 DDS 協定在面對弱訊號或封包抖動時可能出現延遲或卡頓。

* **解決方案**：專案整合了 **Zenoh**（`zenohd` / `rmw_zenoh`）作為輕量化分散式通訊中繼，這也是我們在 JETSEA 中無人船使用的通訊手段。

* **運作機制**：利用 Zenoh 高輸送量、低頻寬佔用且適應長距離無線網橋的特性，確保多節點與遠端地面站之間的遙測數據即時同步。

---

### 8.3 MOOS 與 ROS 2 的跨平台橋接 (`mbpais_bridge`)

海洋自主系統中，MIT 開發的 **MOOS-IvP** 常被用作高階多載具協同與行為決策（Behavior-Based Autonomy），相信大家已經在新生訓練中略有涉獵。而 **ROS 2** 則擅長感測器驅動、SLAM 與底層控制。

* **模組角色**：`mbpais_bridge` 是 MOOSDB 與 ROS 2 之間的雙向翻譯器。


* **資料流向**：
```
【高階決策】MOOS-IvP (行為決策庫)
       ▲
       │ (變數映射 / mbpais_bridge)
       ▼
【中介控制】ROS 2 / MAVROS (速度與航向 Setpoint)
       ▲
       │ (MAVLink 封包 / Wi-Fi)
       ▼
【船端硬體】BlueOS ➔ ArduRover ➔ Navigator (實體推力輸出)

```



---

### 8.4 控制層封裝與程式化控制 SOP

在專案的 `ros2_ws` 中，將第 7 章的手動指令進一步封裝成自動化節點：

1. **自動連線檢查**：節點啟動時訂閱 `/mavros/state`，確認與船隻心跳連線成功（`connected: true`）。

2. **模式與解鎖自動化**：
    * 透過 Client 呼叫 `/mavros/set_mode` 服務切換至 **`GUIDED` 模式**。

    * 呼叫 `/mavros/cmd/arming` 服務完成 **Arming（解鎖）**。

3. **連續速度發布**：
    * 建立 Publisher 持續向 `/mavros/setpoint_velocity/cmd_vel_unstamped` 主題以 **10 Hz** 頻率發送 `geometry_msgs/Twist`（如線速度 `linear.x` 與轉向角速度 `angular.z`），避免觸發自駕核心的逾時安全保護。